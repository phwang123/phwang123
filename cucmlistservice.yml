---
 - name: Sleep for 300 seconds and continue with play
  wait_for:
    timeout: 30
  delegate_to: localhost
    
- hosts: edcccmpub.hollard.group
  gather_facts: no
  tasks:
    - shell: utils list services
